import { aggregate } from './aggregate';
export type { Options, Result, ProcessedFiles } from './types';
export { WriteFileType, SortBy, SortPartten } from './types';
export { log } from './utils';

export default aggregate;